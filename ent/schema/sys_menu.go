// Code generated by entimport, DO NOT EDIT.

package schema

import (
	"entgo.io/ent"
	"entgo.io/ent/dialect/entsql"
	"entgo.io/ent/schema"
	"entgo.io/ent/schema/edge"
	"entgo.io/ent/schema/field"
	"entgo.io/ent/schema/index"
	"github.com/saas-zero/saas-zero-common/pkg/ent/mixins"
)

type SysMenu struct {
	ent.Schema
}

func (SysMenu) Fields() []ent.Field {
	return []ent.Field{
		field.Enum("menu_type").
			Values("directory", "menu", "button").
			Default("directory").
			Comment("类型：directory-目录，menu-菜单，button-按钮"),
		field.String("name").NotEmpty().MaxLen(128).Comment("名称，不能为空"),
		field.Int64("parent_id").Positive().Default(0).Comment("父级id，不能为空,0为第一级"),
		field.String("component").Optional().Default("").
			Comment("The path of vue file | 组件路径"),
		field.String("path").Optional().Default("").
			Comment("Index path | 菜单路由路径"),
		field.String("icon").
			Comment("Menu icon | 菜单图标"),
		field.Bool("is_redirect").Default(false).Comment("是否重定向"),
		field.String("redirect").Optional().Default("").
			Comment("重定向"),
	}
}
func (SysMenu) Edges() []ent.Edge {
	return []ent.Edge{
		edge.From("roles", SysRole.Type).
			Ref("menus"),
	}
}

func (SysMenu) Mixin() []ent.Mixin {
	return []ent.Mixin{
		mixins.BaseMixin{},
		mixins.CreatedMixin{},
		mixins.UpdatedMixin{},
		mixins.DeletedMixin{},
		mixins.StatusMixin{},
		mixins.RemarkMixin{},
		mixins.SortMixin{},
	}
}

func (SysMenu) Annotations() []schema.Annotation {
	return []schema.Annotation{
		entsql.WithComments(true),
		schema.Comment("menu Table | 菜单表"),
		entsql.Annotation{Table: "sys_menus"}}
}

func (SysMenu) Indexes() []ent.Index {
	return []ent.Index{
		// 复合索引 - 租户+父级ID+排序，用于菜单树查询
		index.Fields("parent_id", "sort"),

		// 复合索引 - 租户+路径，用于路径查询
		index.Fields("path"),

		// 复合索引 - 租户+名称，用于名称查询
		index.Fields("name"),

		// 单字段索引 - 父级ID，用于查询子菜单
		index.Fields("parent_id"),

		// 复合索引 - 租户+菜单类型+状态，用于获取有效菜单
		index.Fields("menu_type", "status"),
	}
}
